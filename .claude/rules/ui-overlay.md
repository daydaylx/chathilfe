# UI overlay rules

- Follow `docs/VISUAL_SCOPE.md` for overlay UI work.
- The first overlay state is the narrow Input-Bar.
- The Result-Panel appears only after suggestions exist.
- Show one suggestion at a time.
- Use swipe, arrows, or pager navigation for the three suggestions.
- Do not build a dashboard or a large first-state form.